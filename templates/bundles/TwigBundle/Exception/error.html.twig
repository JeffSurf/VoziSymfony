{% extends 'base.html.twig' %}

{% block body %}
    <div class="d-flex align-items-center justify-content-center vh-100">
        <div class="text-center">
            <h1 class="display-1 fw-bold">{{ status_code }}</h1>
            <p class="fs-3"> <span class="text-danger">Coucou!</span> As-tu trouvé le magot ?</p>
            <p class="lead">
                {{ exception.message ?? status_text }}
            </p>
            <a href="{{ path('home') }}" class="btn btn-primary">Oubliez tout</a>
        </div>
    </div>
{% endblock %}