<header>
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path("home") }}">Vozi</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ path('app_application_lister') }}">Applications</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_eleve_lister') }}">Elèves</a>
                    </li>
                </ul>
                <div class="navbar-nav ms-auto mb-2 mb-lg-0 me-5">
                {% if is_granted("ROLE_USER") %}
                    <div class="nav-item dropdown d-flex">
                        <a class="nav-link dropdown-toggle d-flex align-items-center no-arrow" href="#" id="navbarDropdownMenuLink"
                           role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <span class="mr-2 d-none d-lg-inline text-gray-600 small">{{ app.user.pseudo }}</span>
                            <img src="{{ app.user.image ? asset('uploads/'~app.user.image) : asset('build/images/avatarDefault.png') }}" class="img-profile rounded-circle ms-1"
                                 alt="Avatar" loading="lazy" height="2rem" width="2rem" />
                        </a>
                        <ul class="dropdown-menu dropdown-menu-right shadow animated--grow-in" aria-labelledby="navbarDropdownMenuLink">
                            {{ include('fragments/menuProfil.html.twig') }}
                        </ul>
                    </div>
                {% else %}
                    <a class="nav-link d-flex" href="{{ path('app_login') }}">Se connecter</a>
                {% endif %}
                </div>
            </div>
        </div>
    </nav>
</header>